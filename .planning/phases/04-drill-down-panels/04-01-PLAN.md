---
phase: 04-drill-down-panels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - p-ronsplit-v2/src/components/analytics/TransactionList.tsx
  - p-ronsplit-v2/src/components/analytics/CategoryDrillDown.tsx
  - p-ronsplit-v2/src/components/analytics/index.ts
autonomous: true

must_haves:
  truths:
    - "TransactionList filters and displays expenses by category"
    - "CategoryDrillDown renders Sheet on desktop (>=768px)"
    - "CategoryDrillDown renders Drawer on mobile (<768px)"
    - "Drawer has max-height and scroll support for long lists"
    - "Close button meets 44px touch target minimum"
  artifacts:
    - path: "p-ronsplit-v2/src/components/analytics/TransactionList.tsx"
      provides: "Filtered expense list display"
      exports: ["TransactionList"]
    - path: "p-ronsplit-v2/src/components/analytics/CategoryDrillDown.tsx"
      provides: "Responsive drill-down panel"
      exports: ["CategoryDrillDown", "SelectedCategory"]
  key_links:
    - from: "CategoryDrillDown.tsx"
      to: "TransactionList.tsx"
      via: "import and render"
      pattern: "import.*TransactionList"
    - from: "CategoryDrillDown.tsx"
      to: "use-mobile.tsx"
      via: "useIsMobile hook"
      pattern: "useIsMobile"
---

<objective>
Create the responsive CategoryDrillDown component and TransactionList for displaying filtered category transactions.

Purpose: Provide the UI shell for category drill-down that responds to viewport size - Sheet on desktop, Drawer on mobile with swipe-to-dismiss. This plan creates the components; Plan 02 wires them to the chart.

Output: TransactionList.tsx and CategoryDrillDown.tsx components ready for integration.
</objective>

<execution_context>
@/Users/sannagranqvist/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sannagranqvist/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-drill-down-panels/04-RESEARCH.md

# Source files to reference
@p-ronsplit-v2/src/components/ExpenseItem.tsx
@p-ronsplit-v2/src/hooks/use-mobile.tsx
@p-ronsplit-v2/src/components/ui/sheet.tsx
@p-ronsplit-v2/src/components/ui/drawer.tsx
@p-ronsplit-v2/src/components/ui/scroll-area.tsx
@p-ronsplit-v2/src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TransactionList component</name>
  <files>p-ronsplit-v2/src/components/analytics/TransactionList.tsx</files>
  <action>
Create TransactionList component that:
1. Accepts props: expenses (Expense[]), categoryId (string), members (GroupMember[]), optional onEdit callback, optional currentUserId
2. Filters expenses by category using useMemo
3. Renders empty state in Swedish: "Inga transaktioner i denna kategori" centered with py-12 padding
4. Uses ScrollArea component for scrollable content
5. Maps filtered expenses to ExpenseItem components with proper props (expense, members, index, onEdit, currentUserId)
6. Wraps items in divide-y divide-border container for separators

Import types from @/lib/types (Expense, GroupMember). Import ExpenseItem from @/components/ExpenseItem. Import ScrollArea from @/components/ui/scroll-area.

Follow existing naming conventions (named export function TransactionList).
  </action>
  <verify>
TypeScript compiles without errors:
```bash
cd p-ronsplit-v2 && npx tsc --noEmit
```
  </verify>
  <done>TransactionList.tsx exists with proper filtering, empty state, and ScrollArea wrapper</done>
</task>

<task type="auto">
  <name>Task 2: Create CategoryDrillDown responsive component</name>
  <files>p-ronsplit-v2/src/components/analytics/CategoryDrillDown.tsx</files>
  <action>
Create CategoryDrillDown responsive wrapper component:

1. Define SelectedCategory interface exported from file:
   - id: string
   - name: string
   - icon: string

2. Define CategoryDrillDownProps interface:
   - open: boolean
   - onOpenChange: (open: boolean) => void
   - selectedCategory: SelectedCategory | null
   - expenses: Expense[]
   - members: GroupMember[]
   - onEdit?: (expense: Expense) => void
   - currentUserId?: string

3. Use useIsMobile hook from @/hooks/use-mobile

4. Create shared content component (internal) with:
   - Header: flex items-center gap-3 with category icon (text-2xl) and name (text-lg font-semibold)
   - Body: TransactionList with filtered props
   - Pass categoryId from selectedCategory.id to TransactionList

5. For mobile (isMobile = true):
   - Render Drawer with DrawerContent
   - Add className="max-h-[85vh] flex flex-col" to DrawerContent for scroll support
   - Use DrawerHeader with DrawerTitle for accessibility
   - Body in flex-1 overflow-y-auto container
   - Include DrawerClose button with min-h-[44px] min-w-[44px] for touch target compliance

6. For desktop (!isMobile):
   - Render Sheet with SheetContent side="right"
   - Add className="w-full sm:max-w-md flex flex-col" to SheetContent
   - Use SheetHeader with SheetTitle for accessibility
   - Ensure close button (built into SheetContent) meets touch target (add min-h-[44px] min-w-[44px] if needed via extending className on SheetPrimitive.Close)

7. Handle null selectedCategory gracefully (render nothing or return early)

Imports:
- Sheet, SheetContent, SheetHeader, SheetTitle from @/components/ui/sheet
- Drawer, DrawerContent, DrawerHeader, DrawerTitle, DrawerClose from @/components/ui/drawer
- useIsMobile from @/hooks/use-mobile
- TransactionList from ./TransactionList
- Types from @/lib/types

Note: SheetContent already includes X close button, but verify touch target sizing. DrawerContent needs explicit close if user wants button (swipe-to-dismiss is default).
  </action>
  <verify>
TypeScript compiles without errors:
```bash
cd p-ronsplit-v2 && npx tsc --noEmit
```
  </verify>
  <done>CategoryDrillDown.tsx exists with responsive rendering (Sheet on desktop, Drawer on mobile), proper accessibility titles, and 44px touch targets</done>
</task>

<task type="auto">
  <name>Task 3: Export new components from barrel file</name>
  <files>p-ronsplit-v2/src/components/analytics/index.ts</files>
  <action>
Add exports for new components to the analytics barrel file:

```typescript
export { TransactionList } from './TransactionList';
export { CategoryDrillDown } from './CategoryDrillDown';
export type { SelectedCategory } from './CategoryDrillDown';
```

Preserve existing exports in the file.
  </action>
  <verify>
Check exports resolve correctly:
```bash
cd p-ronsplit-v2 && npx tsc --noEmit
```
  </verify>
  <done>TransactionList, CategoryDrillDown, and SelectedCategory type are exported from analytics barrel file</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd p-ronsplit-v2 && npx tsc --noEmit`
2. Files exist at correct paths:
   - p-ronsplit-v2/src/components/analytics/TransactionList.tsx
   - p-ronsplit-v2/src/components/analytics/CategoryDrillDown.tsx
3. Components follow project conventions (named exports, PascalCase)
4. Touch target classes present (min-h-[44px] min-w-[44px] on close buttons)
5. Responsive logic uses useIsMobile hook correctly
</verification>

<success_criteria>
- TransactionList component filters expenses by categoryId and renders ExpenseItem list
- CategoryDrillDown renders Sheet on viewport >= 768px
- CategoryDrillDown renders Drawer on viewport < 768px
- Both variants include accessible title elements
- Close interactions meet 44px touch target minimum
- Components exported from analytics barrel file
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-drill-down-panels/04-01-SUMMARY.md`
</output>
