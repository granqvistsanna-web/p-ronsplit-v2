---
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - src/components/filters/FilterBar.tsx
  - src/components/filters/DateRangeFilter.tsx
  - src/components/filters/index.ts
  - src/pages/Analys.tsx
autonomous: true
---

# Plan 01-03: Create FilterBar and integrate into Analys.tsx

## Objective
Build complete FilterBar component combining date and member filters, then integrate URL-based filtering into Analys page by replacing existing month navigation.

## Tasks

<task id="1">
Create src/components/filters/DateRangeFilter.tsx component:
- Import DatePreset and DATE_PRESETS from '@/lib/datePresets'
- Props interface: { value: DatePreset, onChange: (preset: DatePreset) => void, disabled?: boolean }
- Import shadcn Select components and Calendar icon from lucide-react
- Render Select with all 7 presets from DATE_PRESETS object
- SelectTrigger shows Calendar icon + SelectValue, className: "h-10 min-w-[180px]"
- Add aria-label="Välj tidsperiod"
- Map over Object.entries(DATE_PRESETS) to create SelectItem for each preset with Swedish label
</task>

<task id="2">
Create src/components/filters/FilterBar.tsx container component:
- Import useFilterParams hook, DateRangeFilter, MemberFilter, Button, RotateCcw icon
- Import GroupMember type from '@/lib/types'
- Props interface: { members: GroupMember[], loading?: boolean }
- Destructure from useFilterParams: { datePreset, setDatePreset, memberIds, setMemberIds, hasActiveFilters, resetFilters }
- Return div with role="group" aria-label="Analysfilter"
- Layout: flex flex-wrap items-center gap-3 (responsive, filters wrap on mobile)
- Render DateRangeFilter with value={datePreset} onChange={setDatePreset} disabled={loading}
- Render MemberFilter with members={members} selectedIds={memberIds} onSelectionChange={setMemberIds} disabled={loading}
- Conditionally render Reset button when hasActiveFilters is true:
  - Button variant="ghost" size="sm" className="h-10 text-sm text-muted-foreground hover:text-foreground"
  - Show RotateCcw icon with label "Återställ" (Swedish for Reset)
  - onClick calls resetFilters
</task>

<task id="3">
Create src/components/filters/index.ts barrel export:
- Export FilterBar, DateRangeFilter, and MemberFilter
</task>

<task id="4">
Update src/pages/Analys.tsx to use URL-based filtering:
- Add imports: import { FilterBar } from '@/components/filters' and import { useFilterParams } from '@/hooks/useFilterParams'
- Add useFilterParams hook near top of component: const { dateRange, memberIds } = useFilterParams()
- REMOVE old state: delete useState for selectedYear and selectedMonth (lines ~30-31)
- REMOVE old navigation functions: delete goToCurrentMonth, goToPreviousMonth, goToNextMonth, handleMonthYearChange, currentMonthYearValue (lines ~162-195)
- REMOVE keyboard navigation useEffect (lines ~199-227)
- REMOVE monthYearOptions useMemo (lines ~230-252)
- UPDATE filteredData useMemo to filter by dateRange and memberIds:
  - For expenses: const matchesDate = expenseDate >= dateRange.start && expenseDate <= dateRange.end
  - For expenses: const matchesMember = memberIds.length === 0 || memberIds.includes(e.paid_by)
  - For incomes: const matchesMember = memberIds.length === 0 || memberIds.includes(i.user_id)
  - Apply both filters: return matchesDate && matchesMember
- UPDATE previousMonthTotals calculation or simplify/remove it (can be added in later phase)
- UPDATE monthlyTrend to calculate 6 months ending at dateRange.end instead of using selectedYear/selectedMonth
- REPLACE old Time Filter section (lines ~310-416) with new FilterBar:
  - <div className="mb-6 animate-fade-in" style={{ animationDelay: '20ms' }}><FilterBar members={household?.members || []} loading={loading} /></div>
- UPDATE category breakdown card heading to show formatted date range instead of MONTHS[selectedMonth - 1]
</task>

## Verification
- [ ] FilterBar renders with DateRangeFilter and MemberFilter side by side
- [ ] Reset button appears only when filters deviate from defaults
- [ ] Selecting date preset updates URL with ?range= parameter
- [ ] Selecting members updates URL with ?members= parameter (comma-separated)
- [ ] Charts and data filter correctly based on dateRange and memberIds
- [ ] Copying URL and opening in new tab shows same filtered view
- [ ] Browser back button restores previous filter state
- [ ] Browser forward button advances filter state
- [ ] Reset button clears URL params and returns to defaults
- [ ] Old month navigation UI is completely removed
- [ ] No console errors or React warnings
- [ ] TypeScript compiles successfully
- [ ] Loading state disables filters

## must_haves
- [ ] FILT-01: User can select date range from presets
- [ ] FILT-02: User can filter by household member via multi-select
- [ ] FILT-03: Reset link appears when filters deviate from default and clears on click
- [ ] Success criteria 1: User can select date range from presets
- [ ] Success criteria 2: User can filter by household member (default: all members)
- [ ] Success criteria 3: URL updates when filters change, making views shareable
- [ ] Success criteria 4: Reset link appears when filters deviate and returns to initial state
- [ ] Success criteria 5: Browser back/forward buttons work correctly with filter changes
