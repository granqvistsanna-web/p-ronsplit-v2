---
phase: 03-category-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/categoryUtils.ts
  - src/components/analytics/CategoryBarChart.tsx
autonomous: true

must_haves:
  truths:
    - "Category bar chart displays spending per category for selected date range"
    - "Chart shows top 8 categories by default sorted by amount descending"
    - "Chart renders correctly on mobile devices (no invisible/collapsed charts)"
    - "Swedish currency formatting displayed in chart tooltips"
  artifacts:
    - path: "src/lib/categoryUtils.ts"
      provides: "Category aggregation from expenses array"
      exports: ["aggregateByCategory", "CategoryData"]
    - path: "src/components/analytics/CategoryBarChart.tsx"
      provides: "Bar chart visualization component"
      exports: ["CategoryBarChart"]
  key_links:
    - from: "src/components/analytics/CategoryBarChart.tsx"
      to: "src/lib/categoryUtils.ts"
      via: "import aggregateByCategory"
      pattern: "import.*aggregateByCategory.*from.*categoryUtils"
    - from: "src/components/analytics/CategoryBarChart.tsx"
      to: "@/components/ui/chart"
      via: "ChartContainer wrapper"
      pattern: "ChartContainer.*className.*min-h"
---

<objective>
Create category data aggregation utility and bar chart component for spending visualization.

Purpose: Enable users to see their spending breakdown by category at a glance, with the highest-spending categories prominently displayed.

Output:
- `src/lib/categoryUtils.ts` - aggregation function transforming expenses into chart-ready data
- `src/components/analytics/CategoryBarChart.tsx` - Recharts BarChart with mobile-safe height constraints
</objective>

<execution_context>
@/Users/sannagranqvist/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sannagranqvist/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-category-visualization/03-RESEARCH.md

# Existing patterns to follow
@p-ronsplit-v2/src/components/analytics/TrendChart.tsx
@p-ronsplit-v2/src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create category aggregation utilities</name>
  <files>src/lib/categoryUtils.ts</files>
  <action>
Create `src/lib/categoryUtils.ts` with:

1. **CategoryData interface:**
```typescript
export interface CategoryData {
  categoryId: string;
  categoryName: string;
  amount: number;
  color: string;
  icon: string;
}
```

2. **aggregateByCategory function:**
- Input: `Expense[]` from useExpenses hook
- Use `Array.reduce()` to group expenses by category, summing amounts
- Map to CategoryData using DEFAULT_CATEGORIES for metadata (name, color, icon)
- Handle unknown categories by falling back to 'ovrigt'
- Sort result by amount descending (highest spending first)
- Return `CategoryData[]`

3. **Type imports:**
- Import `Expense` from `@/lib/types`
- Import `DEFAULT_CATEGORIES` from `@/lib/types`

**Pattern reference:** See RESEARCH.md Pattern 2 for reduce() aggregation pattern.

**Important:** Return empty array for empty expenses input (don't throw).
  </action>
  <verify>
Create temporary test in console or add inline comment showing:
- Empty array returns empty array
- Single expense aggregates correctly
- Multiple expenses in same category sum correctly
- Results sorted by amount descending
  </verify>
  <done>
`aggregateByCategory([])` returns `[]` and `aggregateByCategory(expenses)` returns sorted CategoryData array with correct sums.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CategoryBarChart component</name>
  <files>src/components/analytics/CategoryBarChart.tsx</files>
  <action>
Create `src/components/analytics/CategoryBarChart.tsx` following TrendChart patterns:

1. **Component signature:**
```typescript
interface CategoryBarChartProps {
  expenses: Expense[];
  showAll?: boolean;
}

export function CategoryBarChart({ expenses, showAll = false }: CategoryBarChartProps)
```

2. **Data processing with useMemo:**
- Call `aggregateByCategory(expenses)` inside useMemo with `[expenses]` dependency
- Apply top 8 slice: `showAll ? categoryData : categoryData.slice(0, 8)`

3. **Chart configuration:**
- `chartConfig` with `amount` key for theming (use `hsl(var(--chart-1))`)
- Follow TrendChart chartConfig pattern

4. **ChartContainer setup (CRITICAL for mobile):**
- Use `className="min-h-[300px] w-full"` - explicit min-height prevents collapse
- Wrap with ResponsiveContainer `width="100%" height="100%"`

5. **BarChart component:**
- `margin={{ top: 20, right: 8, left: 0, bottom: 60 }}` (extra bottom for angled labels)
- CartesianGrid: `strokeDasharray="3 3" vertical={false}` matching TrendChart
- XAxis: `dataKey="categoryName"`, `angle={-45}`, `textAnchor="end"`, `height={60}`
- YAxis: tickFormatter for "k" suffix on thousands, matching TrendChart pattern
- Bar: `dataKey="amount"`, `radius={[4, 4, 0, 0]}`, `fill="var(--color-amount)"`

6. **Custom tooltip (CustomTooltip component):**
- Match TrendChart tooltip styling exactly (border, bg-card/95, backdrop-blur-sm, shadow-notion-lg)
- Show category name as header
- Show amount with Swedish formatting: `amount.toLocaleString('sv-SE') + ' kr'`
- Include category icon from payload data

7. **Empty state:**
- If `categoryData.length === 0`, return centered "Ingen data att visa" message
- Use same height container to prevent layout shift

8. **Exports:**
- Named export: `export function CategoryBarChart`
- Also export CategoryBarChartProps interface

**Style matching:** Follow TrendChart exactly for:
- Font: `fontFamily: "Geist Mono"` on axis ticks
- Colors: `hsl(var(--muted-foreground))` for axis text
- Grid: `hsl(var(--border))` with 0.5 opacity
  </action>
  <verify>
Run `npm run dev` and temporarily import CategoryBarChart in Analys.tsx with test data:
```typescript
const testData = [
  { id: '1', amount: 5000, category: 'mat', ... },
  { id: '2', amount: 3000, category: 'transport', ... },
];
<CategoryBarChart expenses={testData} />
```
Verify:
1. Chart renders with bars visible
2. Labels show angled category names
3. Tooltip appears on hover with Swedish currency format
4. Chart does not collapse on mobile viewport (use DevTools responsive mode)
  </verify>
  <done>
CategoryBarChart renders bar chart with category spending, showing top 8 by default, with mobile-safe height constraints (min-h-[300px]), Swedish currency formatting in tooltip, and angled X-axis labels.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` succeeds without type errors
2. CategoryBarChart component exists and exports correctly
3. aggregateByCategory function exports correctly
4. No ESLint warnings about unused exports
</verification>

<success_criteria>
- [ ] `src/lib/categoryUtils.ts` exists with aggregateByCategory function
- [ ] `src/components/analytics/CategoryBarChart.tsx` exists with CategoryBarChart component
- [ ] Chart has explicit min-height (300px) preventing mobile collapse
- [ ] Chart shows categories sorted by spending amount descending
- [ ] Tooltip displays Swedish-formatted currency
- [ ] Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-category-visualization/03-01-SUMMARY.md`
</output>
